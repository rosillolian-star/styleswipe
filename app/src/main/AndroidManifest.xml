<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.styleswipe">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:roundIcon="@drawable/icon"
        android:label="StyleSwipe"
        android:theme="@style/Theme.Styleswipe">

        <!-- OutfitListActivity is now the main hub/home screen -->
        <activity
            android:name=".OutfitListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".CaptureActivity" />
        <activity android:name=".OutfitDetailActivity" />
        <activity android:name=".AlbumDetailActivity" />
        <activity android:name=".OutfitPickerActivity" />

    </application>
</manifest>
